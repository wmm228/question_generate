# Workspaces

Optional workspace seeds live here.

`pnpm storage:sync` does not publish this directory unless you pass `--include-workspaces`.

Leave it empty if you want to create workspaces later through the API or UI.
