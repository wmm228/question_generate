请根据评估反馈修订这道试题。

已确认规范：
{spec_json}

上一版草稿 JSON：
{draft_json}

评估问题：
{issues_json}

修订要求：
{revision_instructions}

只返回一个修订后的合法 JSON 对象。
- 不要使用 Markdown 代码块。
- 不要在 JSON 之外输出任何解释性文字。
- 不要在前后添加推理、说明、标签或注释。

原始生成约束：
{generation_constraints}
